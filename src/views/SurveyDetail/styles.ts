import { Text } from "@hudoro/neron";
import styled from "styled-components";

export const PeriodeText = styled(Text).attrs({
	variant: "h4",
})`
	font-weight: 400;
	font-size: 18px;
	line-height: 36px;
`;
